/** Hauteur par défaut des murs */
export const DEFAULT_WALL_HEIGHT = 4;

/** Couleur par défaut du sol */
export const DEFAULT_FLOOR_COLOR = "#2a2a3a";

/** Couleur par défaut des murs */
export const DEFAULT_WALL_COLOR = "#3a3a4a";

/** Largeur par défaut des ouvertures de portes */
export const DEFAULT_DOOR_WIDTH = 1.5;

/** Ratio par défaut hauteur porte / hauteur mur */
export const DEFAULT_DOOR_HEIGHT_RATIO = 2 / 3;

/** Chemin vers le modèle de mur */
export const WALL_MODEL_PATH = "/models/animated/empty_wall.glb";

/** Chevauchement entre segments de murs adjacents */
export const WALL_OVERLAP = 1 / 3;

/** Offset des murs par rapport aux bords de la pièce */
export const WALL_OFFSET = 0.25;
